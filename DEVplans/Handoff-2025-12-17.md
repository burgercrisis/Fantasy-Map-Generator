---
status: handoff
area: no-unique-base2
createdAtUtc: 2025-12-17T06:59:11.053Z
---

## /no-unique-base2 continuation (blocked)

Goal: Run the `/no-unique-base2` verification checklist (guardrails/check-deltas/seed-uniqueness/coverage/failures/base-clusters) and report results.

### What ran
- `pnpm run mixer:guardrails` => OK (`[guardrails] OK. map=3498 catalog=3498`)
- `pnpm run mixer:check-deltas` => FAIL
  - Reason: `[apply-mixer-deltas] Artifacts are out of date vs deltas: config/language-mixer-map.json (and config/language-mixer-map.js) need update`
- Targeted batch verification (most recent completed batch in `tools/mixer-diagnostics/_no_uniq_base_claims.json`):
  - ISOs: `bayot, bbc, bbh, bcc, bdz`
  - `pnpm exec -- node tools/mixer-diagnostics/report-language-mixer-seed-uniqueness.js --only-failures "--only-isos=bayot,bbc,bbh,bcc,bdz" --limit=300` => OK
    - Target ISOs: 5
    - Missing mapping: 0
    - No globally-unique base index: 0
    - Strict unique seeds below threshold (among those with unique base): 0
    - Normalized unique seeds below threshold (among those with unique base): 0
- `pnpm exec -- node tools/mixer-core/check-language-mixer-coverage.js` => OK
  - ISOs in map but missing from catalog: 0
  - ISOs in catalog but missing from map: 0
- `pnpm exec -- node tools/mixer-core/check-language-mixer-failures.js` => OK
  - Languages that will fail in local mixer: 0
- `pnpm exec -- node tools/mixer-diagnostics/report-language-mixer-base-clusters.js --min-size=2` => OK
  - Clusters with identical base sets (size >= 2): 109
  - Total language entries participating in these clusters: 692

### Why blocked
To make `mixer:check-deltas` pass we must run `pnpm run mixer:apply-deltas` (regenerates artifacts). That requires hub locks for the files it writes.

### Lock evidence
Current UTC (observed): `2025-12-17T07:04:39.559Z`

Lock holder for `file:tools/mixer-deltas/_compiled-dedicated-pins.json` at time of this note:
- `workstream_id=81220be5-e4ea-4b62-9c54-e152d25a96bd` (`decluster-language-bases-2025-12-17`)
- `expires_at` epochMs=1765956750730

Notes:
- `file:config/language-mixer-map.json` and `file:config/language-mixer-map.js` were temporarily lock-free and were acquired, but were released immediately after discovering the compiled pins lock to avoid blocking other work.

### Next steps (when `_compiled-dedicated-pins.json` lock is free)
1) Acquire locks:
   - `file:config/language-mixer-map.json`
   - `file:config/language-mixer-map.js`
   - `file:config/language-mixes-all.js`
   - `file:tools/mixer-deltas/_compiled-dedicated-pins.json`
2) Run:
   - `pnpm run mixer:apply-deltas`
   - `pnpm run mixer:check-deltas`
   - `pnpm exec -- node tools/mixer-diagnostics/report-language-mixer-seed-uniqueness.js --only-failures "--only-isos=bayot,bbc,bbh,bcc,bdz" --limit=300`
   - `pnpm exec -- node tools/mixer-core/check-language-mixer-coverage.js`
   - `pnpm exec -- node tools/mixer-core/check-language-mixer-failures.js`
   - `pnpm exec -- node tools/mixer-diagnostics/report-language-mixer-base-clusters.js --min-size=2`

Notes:
- No repo files were modified during this attempt.
