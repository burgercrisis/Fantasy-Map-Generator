---
status: handoff
area: no-unique-base2
createdAtUtc: 2025-12-17T06:59:11.053Z
---

## /no-unique-base2 verification (completed)

Goal: Run the `/no-unique-base2` verification checklist (guardrails/check-deltas/seed-uniqueness/coverage/failures/base-clusters) and report results.

### What ran
- `pnpm run mixer:guardrails` => OK (`[guardrails] OK. map=3498 catalog=3498`)
- `pnpm run mixer:check-deltas` => OK
- `pnpm exec -- node tools/mixer-diagnostics/report-language-mixer-seed-uniqueness.js --only-failures --limit=300` => OK
  - Target ISOs: 3397
  - Missing mapping: 0
  - No globally-unique base index: 2259
  - Strict unique seeds below threshold (among those with unique base): 16
  - Normalized unique seeds below threshold (among those with unique base): 172
- `pnpm exec -- node tools/mixer-core/check-language-mixer-coverage.js` => OK
  - ISOs in map but missing from catalog: 0
  - ISOs in catalog but missing from map: 0
- `pnpm exec -- node tools/mixer-core/check-language-mixer-failures.js` => OK
  - Languages that will fail in local mixer: 0
- `pnpm exec -- node tools/mixer-diagnostics/report-language-mixer-base-clusters.js --min-size=2` => OK
  - Clusters with identical base sets (size >= 2): 109
  - Total language entries participating in these clusters: 692

### Remaining blocker
`DEVplans/Languages-Status.md` is hub-locked, so the status-only verification note could not be recorded there yet.

### Next batch queued (Americas Indigenous list uniqueness)
Read-only diagnostics (no-devplan) show `tools/mixer-meta/wikipedia-indigenous-languages-of-the-americas.json` is fully wired (221/221) but has `Nonunique Bases: 169`.

High-impact next batch (Na-Dene / North America): these 11 ISOs share `bases=[19]`:
- `eyak`
- `tfn`
- `chp`
- `ing`
- `gwi`
- `haa`
- `hoi`
- `koy`
- `dgr`
- `kuu`
- `tau`

Planned fix approach:
- Create a `dedicatedPins` delta for these 11 ISOs mapping each to a new globally-unique base index.
- Append 11 dedicated base definitions (append-only) in an appropriate `modules/namebases-*.js` file.
- Run verification (at minimum): `pnpm run mixer:guardrails`, `pnpm run mixer:check-deltas`, and re-run the list base-uniqueness report with `--no-devplan`.

Notes:
- No repo files were modified during this attempt.
