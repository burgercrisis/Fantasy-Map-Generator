# Handoff (2025-12-15)

## Scope / context
Language mixer uniqueness work (Africa hub bases `112/113`) + tooling sanity checks.
Language mixer history audit: investigate the persistent `Loss counts (history minus current) = 180` from `tools/mixer-core/report-language-history-totals.js` and determine whether the “lost” ISO keys represent renames/canonicalization vs true deletion.

## Status-only recap (last ~12 hours)

### Language mixer: uniqueness / claims
- Multiple NO_UNIQ_BASE2 micro-passes were completed (dedicated pins + dedicated bases + verification), and their claims were marked complete with DEVplans status entries.
- There is at least one NO_UNIQ_BASE2 batch that was left as `in_progress` at last check:
  - batchId: `2025-12-15T11:21:35.186Z-worker1`
  - isos: `ais`, `ait-seghrouchen-berber`, `aiton`, `ajawa-language`, `akan`
  - reservedRange: `2519–2568`

### Language mixer: tooling / gates
- `pnpm run mixer:check-deltas` was observed in a "artifacts out of date" state at one point, then a subsequent `pnpm run mixer:apply-deltas` was run and `mixer:check-deltas` was confirmed passing again.
- Ongoing caution remains around duplicate / missing base definitions (especially when dedicated pins reference indices that must exist exactly once across `modules/namebases-*.js`).
- Verified `pnpm run mixer:guardrails` and `pnpm run mixer:check-deltas` passing after removing duplicate `i:2900–2904` base entries and a stray markdown fence from `modules/namebases-real.js`.

### Wikipedia list work (/wikipedia1)
- Continued progress on declustering / fully representing targeted Wikipedia lists (coverage improvements and/or Nonunique Bases reductions), with some lists reaching 100% fully-wired coverage.

### Decluster coordination
- A decluster claim + staged delta was prepared for a base-set collision involving bases `[47,56]` affecting ISOs `tdh` and `ola` (delta staged; integrator lane needed for apply-deltas).

### Races/cultures decoupling (Choice A)
- Work progressed toward making `pack.cells.race` authoritative (including persistence on save/load and name generation using per-cell base resolution). Additional verification and follow-up wiring remains.

## What was completed
### Africa `112/113` burn-down (batch5)
- Delta file exists:
  - `tools/mixer-deltas/2025-12-15-worker39-africa-112-113-batch5.json`
  - Dedicated pins (11 ISOs):
    - `wapan:2424`, `weh:2425`, `wushi:2426`, `yemba:2427`, `zande:2428`, `yor:2429`, `wannu:2430`, `yalunka:2431`, `yamba:2432`, `yela-kela:2433`, `yulu:2434`
- `modules/namebases-real.js` contains the dedicated base definitions for `i:2424–2434` (append-only).
- Generated artifacts already include the pins (so batch5 is effectively applied already):
  - `config/language-mixer-map.json`:
    - `wapan` bases include `2424`
    - `yemba` bases include `2427`
    - `yela-kela` bases include `2433`
    - `yor` bases include `2429`
  - `tools/mixer-deltas/_compiled-dedicated-pins.json`:
    - contains `wapan: 2424`
    - contains `yor: 2429`

### DEVplans updates made
- `DEVplans/Languages-Status.md`
  - Added / then corrected the Africa `112/113` batch5 status line.
  - Current line is ✅ **2025-12-15 (verified)** and notes that pins are present in `config/language-mixer-map.json` and `_compiled-dedicated-pins.json`.

## Verification evidence (read-only)
- `node --check modules/namebases-real.js` => exit 0
- `pnpm exec -- node tools/mixer-core/apply-mixer-deltas.js --check --no-lock` => clean (guardrails OK)

## Language mixer: 180 “lost ISO” audit (history minus current)

### What was run (read-only / diagnostics)
- Regenerated the loss artifacts (does not change default behavior/output):
  - `pnpm exec -- node tools/mixer-core/report-language-history-totals.js --write-losses --write-losses-meta`

### Where the loss lists live
- Concrete list (180 ISOs):
  - `tools/mixer-diagnostics/_language-history-losses.json`
- Last-seen commit metadata per ISO + commit summary:
  - `tools/mixer-diagnostics/_language-history-losses-meta.json`
- Totals JSON output:
  - `tools/mixer-diagnostics/_language-history-totals.json`

### Current totals (latest rerun)
- Commits scanned: `173`
- History totals: `all ISOs = 3678`
- Current totals: `all ISOs = 3498`
- Loss counts (history minus current): `fromAll = 180` (same in catalog+map)

### Key finding from loss-meta
- The dominant last-seen commit for the losses is:
  - `1c0c908b3ee68b08cd0beecde1e1226151ec2569` (most losses last seen here in `config/language-mixer-map.json`)

### Sample triage (representative lost keys)
The lost ISO keys include short aliases like `hau` and `heb`.

- Confirmed they existed historically:
  - `git grep -n '"iso": "hau"' 1c0c908b3ee68b08cd0beecde1e1226151ec2569 -- config/language-mixer-map.json`
  - `git grep -n '"iso": "heb"' 1c0c908b3ee68b08cd0beecde1e1226151ec2569 -- config/language-mixer-map.json`
- Confirmed canonical entries exist in current map:
  - `"iso": "hausa"` exists
  - `"iso": "hebrew"` exists

### Exact removal commit identified (hau/heb)
The first commit after `1c0c...` that touches `config/language-mixer-map.json` is:
- `5f518ab28af42e55b9f3f20ce83f38d6ce5a113f` (`refactor(language-mixer): refine data and add analysis tools`, 2025-12-08)

In that commit’s diff, the following removals were observed:
- Removed `"iso": "hau"` (multiple occurrences)
- Removed `"iso": "heb"` (multiple occurrences)

This strongly suggests at least some portion of the “losses” are **alias cleanup/canonicalization** rather than the language concept disappearing (but the audit counts it as “loss” because the key itself is gone).

### Decision needed (policy)
Given `DEVplans/Languages-Status.md` defines `config/language-mixes.json` and `config/language-mixer-map.json` as **append-only registries**, the team needs to choose one of:

- Restore legacy alias keys (treat as data loss; keep backwards compatibility)
- Accept alias deletion and redefine “append-only” to apply only to canonical concepts/IDs
- Add an explicit redirect/alias layer instead of keeping aliases in the main registries

### Next worker: suggested next steps
1) Classify a wider sample (10–20) from `_language-history-losses.json` into:
   - renamed/canonicalized alias
   - merged/moved macro key
   - truly deleted (no obvious replacement)
2) If append-only-by-key is still required, restore aliases via a minimal delta strategy (or explicit registry additions) rather than rollback.

## Important constraint reminders
- Single-integrator lane is in effect:
  - Only integrator should run `pnpm run mixer:apply-deltas` (writes/regenerates artifacts).
  - Non-integrators may run `pnpm exec -- node tools/mixer-core/apply-mixer-deltas.js --check` for read-only validation.
  - Workflow: `.windsurf/workflows/single-integrator-lane.md`

## What is NOT done (pending)
### NO_UNIQ_BASE2 / worker65 unblock (mixer:check-deltas)
`pnpm run mixer:check-deltas` was failing due to missing base definitions + a dedicated-pin conflict around the Oceania dedicated bases.

Current repo state (changes already made, but **not yet re-verified** end-to-end after the last edits):
- `modules/namebases-creole.js`
  - Removed duplicate dedicated base definitions for `i:1550–1553` (these must be defined only once globally).
- `modules/namebases-real.js`
  - Restored the dedicated base definitions needed by existing pins/map:
    - `i:1550–1553` (Yapese/Maisin/Norfuk/Pitkern)
    - `i:1593–1594` (Southeast Metafonetica / Southern Aragonese)
    - `i:1640–1641` (Surmiran / Sursilvan)
  - File currently ends with `];` (array closed).
- `tools/mixer-deltas/_compiled-dedicated-pins.json`
  - Should have Oceania pins aligned to the delta:
    - `yap:1550`, `mbq:1551`, `norfuk:1552`, `pitkern:1553`

Immediate next verification commands (run in repo root):
- `pnpm run mixer:check-deltas`

If it still reports missing base definitions, validate what the scanner sees (it uses the regex `\{name:\s*"[^"]+",\s*i:\s*(\d+)` inside `tools/mixer-core/apply-mixer-deltas.js`):
- `node -e "const fs=require('node:fs');const src=fs.readFileSync('modules/namebases-real.js','utf8');const re=/\\{name:\\s*\"[^\\"]+\",\\s*i:\\s*(\\d+)/g;let m;const s=new Set();while((m=re.exec(src))) s.add(Number(m[1]));const check=[1550,1551,1552,1553,1593,1594,1640,1641];for(const n of check) console.log(n, s.has(n)?'FOUND':'MISSING');"`

### Africa full-list snapshot re-run
A read-only Africa snapshot command was proposed but **canceled**, so the “post-batch5” `Nonunique Bases`/cluster counts were not re-captured:
- Pending command:
  - `pnpm exec -- node tools/mixer-core/report-wikipedia-list-base-uniqueness.js tools/mixer-meta/wikipedia-languages-of-africa-full.json --no-devplan`
- If results differ materially, add a status-only update in `DEVplans/Languages-Status.md`.

### Next workstream candidate (active claim)
`tools/mixer-diagnostics/_no_uniq_base_claims.json` shows an `in_progress` NO_UNIQ_BASE2 claim:
- workerId: `1`
- batchId: `2025-12-15T11:21:35.186Z-worker1`
- ISOs: `ais`, `ait-seghrouchen-berber`, `aiton`, `ajawa-language`, `akan`
- reservedRange: `2519–2568`
Next worker should either continue this claim or mark it `stalled` with a blocker/handoff note (do not leave stale `in_progress`).

## Notes on tooling issues encountered
- Memory MCP search had intermittent JSON parse errors; used repo JSON files (e.g. `_no_uniq_base_claims.json`) as authoritative for overlap checks.
- Repo tool `grep_search` can fail if the query begins with leading `--...` (ripgrep interprets it as a flag). Use quoted patterns like `"--check"` or search for other tokens.

## Cascade handoff note (2025-12-15 04:18 PT)
### What I verified in this session
- `tools/mixer-core/check-language-mixer-guardrails.js` checks duplicate namebase indices by scanning **all** `modules/namebases-*.js` files (via `listNamebasesFiles()`).
- The files currently matching that glob are:
  - `modules/namebases-all.js`
  - `modules/namebases-creole.js`
  - `modules/namebases-fantasy.js`
  - `modules/namebases-real.js`
- `modules/namebases-all.js` does not define bases; it merges `window.realWorldNameBases`, `window.fantasyNameBases`, `window.creoleNameBases`.
- Quick greps show `i: 2019` and `i: 2033` occur in `modules/namebases-real.js` (I did not find those indices in `namebases-creole.js` or `namebases-fantasy.js`).

## Retirement signoff (Cascade) (2025-12-16)
- Retirement cleanup performed:
  - Confirmed no `tools/mixer-diagnostics/*.lock` files exist.
  - Updated `tools/mixer-diagnostics/_no_uniq_base_claims.json` to avoid leaving an `in_progress` claim:
    - Marked batchId `2025-12-16T09:59:47.059Z-worker1` (isos: `cao-lan,cao-miao,cape-verdean-creole,cappadocian-greek,car-nicobarese`) as `stalled`.
- Current open debt (do not continue unless you explicitly want to):
  - `modules/namebases-real.js` was briefly clobbered in the editor earlier in the session, but appears present/valid at the top-of-file now.
  - There is a stale/stalled NO_UNIQ_BASE2 claim reserving `5106-5155` with a note that it was created accidentally; treat it as reserved but not necessarily correct to implement.
- What the next agent can choose to do:
  - Recommended: run coordination + sanity checks before any edits:
    - `pnpm exec -- node tools/mixer-diagnostics/claims-dashboard.js`
    - `pnpm exec -- node tools/mixer-diagnostics/no-uniq-base-claim.js --dashboard`
    - Then decide whether to re-claim the `5156-5205` batch or abandon it.
  - If you want to finish the most recent reserved batch (`5156-5205`):
    - Ensure dedicated bases `i:5156-5160` exist exactly once across `modules/namebases-*.js`.
    - Add/verify a delta pins file mapping:
      - `cao-lan->5156`, `cao-miao->5157`, `cape-verdean-creole->5158`, `cappadocian-greek->5159`, `car-nicobarese->5160`.
    - Verify per `.windsurf/workflows/no-unique-base2.md`.
  - If you want to resolve the earlier Arabic dialect attempt (`5106-5110`):
    - First confirm the claim notes are still accurate and that the base defs are not duplicated across namebases files.

## README status note (2025-12-15T05:54:25.9791054-08:00)
- `README.md`
  - Converted fork-specific `github.com/.../blob/...` links to relative paths (stability).
  - Consolidated duplicate `**0. Current focus:**` bullets into a single item with sub-links.
  - Removed a stray control character from the “Language mixer helper tools & workflows” line.

## Relative-link sweep status note (2025-12-15T13:27:16.0291917-08:00)
- Verified (repo scan) that there are **no remaining** `github.com/burgercrisis/Fantasy-Map-Generator/...` links in `*.md` files; internal references are now relative.

### What I did NOT complete (still needs verification)
- I did **not** get an authoritative duplicate-index report from guardrails in this session.
  - I proposed a Node one-liner to enumerate duplicate `i:` values across `modules/namebases-*.js`, but the command was **canceled**.
- Shared-memory MCP workstream lookup was attempted twice and failed with the same JSON parse error, so I could not confirm overlap via MCP.

### Concrete next steps for the next worker
- Run guardrails to get the authoritative list of duplicate indices:
  - Preferred (standard scripts): `pnpm run mixer:guardrails`
  - If git is unavailable / you want to skip append-only checks: `node tools/mixer-core/check-language-mixer-guardrails.js --allow-no-git`
- If guardrails reports duplicate indices:
  - Follow the guardrails instruction: **renumber** the newly-added base(s) (append-only) to an unused index.
  - Re-run `pnpm run mixer:guardrails` until it reports OK.
- Once guardrails is clean:
  - Run `pnpm run mixer:apply-deltas`
  - Then `pnpm run mixer:check-deltas`
